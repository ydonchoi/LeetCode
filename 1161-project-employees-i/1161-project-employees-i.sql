# Write your MySQL query statement below
select p.project_id, round(sum(experience_years)/count(experience_years), 2) average_years from project p left outer join employee e on p.employee_id=e.employee_id group by p.project_id